#include <xsimd/xsimd.hpp>

#pragma once

namespace tpa {

using xsimd::abs;
using xsimd::fabs;
using xsimd::fmod;
using xsimd::remainder;
using xsimd::min;
using xsimd::max;
using xsimd::fmin;
using xsimd::fmax;
using xsimd::fdim;
using xsimd::clip;

using xsimd::exp;
using xsimd::exp2;
using xsimd::exp10;
using xsimd::expm1;
using xsimd::log;
using xsimd::log2;
using xsimd::log10;
using xsimd::log1p;

using xsimd::pow;
using xsimd::rsqrt;
using xsimd::sqrt;
using xsimd::cbrt;
using xsimd::hypot;

using xsimd::sin;
using xsimd::cos;
using xsimd::sincos;
using xsimd::tan;
using xsimd::asin;
using xsimd::acos;
using xsimd::atan;
using xsimd::atan2;

using xsimd::sinh;
using xsimd::cosh;
using xsimd::tanh;
using xsimd::asinh;
using xsimd::acosh;
using xsimd::acosh;

using xsimd::erf;
using xsimd::erfc;
using xsimd::tgamma;
using xsimd::lgamma;

using xsimd::ceil;
using xsimd::floor;
using xsimd::trunc;
using xsimd::round;
using xsimd::nearbyint;
using xsimd::rint;

}
